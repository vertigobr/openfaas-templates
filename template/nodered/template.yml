language: nodered
fprocess: node index.js
welcome_message: |
  You have created a new function which uses NodeRed and the OpenFaaS
  nodered documentation: https://nodered.org/docs/
  of-watchdog which gives greater control over HTTP responses.
  npm i --save can be used to add third-party packages like request or cheerio
  npm documentation: https://docs.npmjs.com/
  Unit tests are run at build time via "npm test", edit package.json to specify 
  how you want to execute them.
